import Cell from cell
import Edge from libs:graph


Muscle extends Edge {
	
	.constructor(a, b, dist) {
		.super(a, b, dist);
	}
	
	
	.set(dist) { .dist = dist; }
	
	
	.contract() {
		
		.pull();
		
		// TODO: muscle business
		
	}
	
}
